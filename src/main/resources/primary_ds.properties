# jdbc.X
primary.jdbc.driverClassName=com.mysql.jdbc.Driver
primary.jdbc.url=jdbc:mysql://10.1.8.2:3306/dw_rts_som_serv
#?createDatabaseIfNotExist=true
primary.jdbc.user=aschoeppe
primary.jdbc.pass=

# hibernate.X
primary.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
primary.hibernate.hbm2ddl.auto=validate
#list of possible options are,
#validate: validate the schema, makes no changes to the database.
#update: update the schema.
#create: creates the schema, destroying previous data.
#create-drop: drop the schema when the SessionFactory is closed explicitly, typically when the application is stopped.